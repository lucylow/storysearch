// Stub service to prevent import errors
export const aiWorkflowService = {
  createWorkflow: async () => ({ success: true, data: null }),
  executeWorkflow: async () => ({ success: true, data: null })
};

export default aiWorkflowService;
