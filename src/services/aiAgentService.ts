// Stub service to prevent import errors
export const aiAgentService = {
  processQuery: async () => ({ success: true, data: null }),
  createAgent: async () => ({ success: true, data: null })
};

export default aiAgentService;
